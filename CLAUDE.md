# NanoMatAI — ML-проект по наноэлектронике

> Этот файл читается Claude Code автоматически при запуске в папке проекта.
> Он вводит ассистента в контекст: что за проект, что уже сделано, на чём застряли, куда идём.

---

## Кто я и что у меня есть

- Студент, специализация — **наноэлектроника**. Навыки: Python + ML.
- Железо: **MacBook Air M4** (Apple Silicon, без дискретной GPU; есть MPS).
- GPU при необходимости — через **Kaggle** (бесплатный, открытый интернет).
- Данных собственных пока **нет** — только открытые источники и навыки.

## Что за проект

Изначально была идея большого «ИИ-ассистента для материаловедения» (RAG-чат +
предсказание свойств + анализ спектров + генерация рекомендаций). Решено, что для
соло это слишком широко, и проект **сознательно сужен** до одной узкой вертикали.

**Текущий фокус:** baseline-модель, которая по составу материала предсказывает
**band gap** для **2D-полупроводников** (релевантно наноэлектронике — канальные
материалы, контакты).

Принципы, которых придерживаемся:
- Узкий фокус вместо «платформы для всего».
- Сначала тупой работающий baseline, потом усложнение (GNN) — и только если оно
  реально бьёт baseline.
- **Никакого собственного парсера данных на старте.** Данные берём через готовые
  официальные API (JARVIS-DFT, Materials Project, matminer). Свой сборщик строим
  только если уйдём в вертикаль без API (напр. газовые сенсоры из PDF-статей).
- Честная валидация: train/test + кросс-валидация + оценка неопределённости.
- Преимущество — интерпретируемость и понимание физики, а не побитие SOTA.

## Файлы в проекте

- `baseline_2d_bandgap.py` — composition-baseline. Источники `--source`: jarvis (dft_2d),
  c2db, alex_2d (Alexandria, `--ehull-max`), matminer. Magpie+Stoich+ValenceOrbital →
  RandomForest/XGBoost, MAE/RMSE/R², 5-fold CV, неопределённость, parity-plot. Флаги
  `--drop-metals`, `--ehull-max`. `describe_target()` — сводка таргета.
- `export_structures_for_alignn.py` — экспорт структур в формат ALIGNN/CGCNN
  (`<id>.vasp` + `id_prop.csv`). `--source dft_2d|c2db|alex_2d`, `--ehull-max`.
- `screen_bandgap.py` — ИНСТРУМЕНТ: структура (CIF/POSCAR/.vasp) → band gap + неопредел.
  Локально на CPU. `--app` (Gradio-морда) или `--in <папка> --out results.csv` (батч-
  скрининг с ранжированием). Автодетект весов: `cgcnn_2d_ensemble.pt` (ансамбль 5 моделей,
  unc=std между ними) ИЛИ `cgcnn_2d_bandgap.pt` (одиночная, unc=MC-dropout). Опц.
  `cgcnn_2d_typed.pt` — добавляет тип щели (прямая/непрямая, AUC 0.80). Встроена поправка
  PBE→эксперимент (exp≈1.26·gap−0.12) и трёхуровневый вердикт по неопределённости.
- `validate_experiment.py` — валидация на 7 эталонных монослоях (модель vs эксп. щели),
  PBE-сдвиг, линейная поправка. Структуры из dft_2d по jid.
- `README.md` — лаб-ready резюме проекта (что умеет, метрики, границы, вопросы к лабам).
- `requirements.txt` — зависимости (baseline). Для инструмента: torch, torch_geometric, gradio.

Запуск:
```
python baseline_2d_bandgap.py --source jarvis      # 2D-материалы (основной)
python baseline_2d_bandgap.py --source matminer    # запасной источник
```

## Что уже сделано / статус

- [x] Скрипт написан, логика (фичи → модель → метрики) проверена — работает.
- [x] Окружение на маке поднято через `venv` (Python 3.14).
- [x] Решена ошибка `externally-managed-environment` — поставили всё в venv.
- [x] Решена ошибка XGBoost `libxgboost.dylib / libomp` — нужен `brew install libomp`,
      ЛИБО XGBoost можно убрать (`pip uninstall xgboost`), скрипт работает и без него
      на одном RandomForest (импорт обёрнут в try/except).
- [x] **РЕШЕНО (блокер сети):** падало на скачивании с figshare/AWS из-за локального
      прокси `http://127.0.0.1:1082` (отдавал 503). Лечится запуском без прокси:
      `env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy ./venv/bin/python ...`
      Данные закешированы в `~/.jarvis`, повторно качать не нужно.
- [x] **Получены реальные метрики baseline** (composition-only, 2D band gap).
- [x] Добавлены `describe_target()` (сводка/гистограмма таргета) и флаг `--drop-metals`.

## Реальные метрики baseline (1103 материала JARVIS dft_2d)

Распределение таргета: **37% — металлы (gap=0)**, остальное полупроводники (med≈0.81 эВ).

| Метрика | Все материалы | Только полупроводники (`--drop-metals`) |
|---|---|---|
| R² | 0.464 | **0.558** |
| RMSE | 0.970 эВ | **0.789 эВ** |
| MAE (test) | 0.614 | 0.599 |
| MAE (5-fold CV) | 0.557 | 0.583 |

**Вывод:** отсечение металлов даёт честную картину (R² 0.46→0.56, RMSE −0.18). Состав
объясняет ~56% дисперсии щели у полупроводников; оставшиеся ~44% — структура (геометрия
слоёв, число слоёв, деформация), которую композиция физически не видит. После отсечения
в топе важности уходит доминанта электроотрицательности (она разделяла металл/не-металл),
вперёд выходят `MeltingT`, `CovalentRadius`, `NdValence` — прочность связи и размер атомов.

Это казалось **сигналом к переходу на структурные модели (GNN)**. НО: GNN-проверка
ниже показала, что на 696 точках структура НЕ бьёт состав — узким местом оказался
объём данных, а не модель. См. раздел про GNN.

## Переход на GNN — итог: ушли с ALIGNN/DGL на PyG/CGCNN

**Экспорт данных:** `export_structures_for_alignn.py` → папка `alignn_data/`
(696 полупроводников = та же выборка, что baseline `--drop-metals`; формат:
`<jid>.vasp` + `id_prop.csv` без заголовка `файл,band_gap`, ~2.7 МБ). Заливать в
Kaggle удобно zip-ом (`alignn_data.zip`, ~300 КБ) — Kaggle сам распакует.

### DGL оказался нерабочим на Kaggle — НЕ тратить на него время снова

Пытались поднять ALIGNN (он на DGL). По шагам, что НЕ сработало:
- Kaggle-образ: Python 3.12, **torch 2.10.0+cu128** (очень свежий).
- `pip install alignn` ставится, но **dgl с ним не приходит** (нет на обычном PyPI).
- DGL-колёса под torch 2.10 **не существуют** (макс. `torch-2.4`).
- Понизили torch → 2.4.0+cu121. Тогда: cu121-колесо DGL → **HTTP 403** (CDN
  `data.dgl.ai` сломан), cu118-колесо DGL ставится, но требует `libcudart.so.11.0`
  (**CUDA 11**, которой на Kaggle нет — там 12.8).
- Понижение torch до 2.4 ещё и **ломает сам torch** (`torch._dynamo` →
  `GuardSource has no attribute LOCAL_NN_MODULE`) → лечится Factory reset.

**Вывод: DGL-стек на Kaggle сейчас нежизнеспособен.** Если возвращаться к ALIGNN —
только через conda-окружение с фиксированными torch+cudatoolkit, не через pip.

### Рабочий путь: PyTorch Geometric + CGCNN (без DGL)

PyG ставится чисто на родной torch 2.10, понижать ничего не надо. Грабли:
- **GPU:** P100 — это sm_60, torch 2.10 его НЕ поддерживает (`no kernel image`).
  Брать **T4** (sm_75, `(7,5)`). Не P100.
- `pip install -q torch_geometric pymatgen` (pymatgen — читать `.vasp`). Держать
  install и код в ОДНОЙ ячейке: смена GPU/Factory reset стирают pip-инсталляции.
- Модель: `torch_geometric.nn.CGConv`, эмбеддинг по Z, RBF на рёбрах (cutoff 8 Å,
  `Structure.get_neighbor_list` с PBC), global_mean_pool, нормировка таргета.

### Первый результат GNN (CGCNN, 696 структур, 200 эпох, T4)

| Метрика | baseline (состав) | CGCNN (структура) |
|---|---|---|
| MAE  | **0.599** | 0.670 |
| RMSE | **0.789** | 0.920 |
| R²   | 0.558 | 0.624 |

**ВАЖНО — метрики НЕ сравнивать в лоб:** разные тест-сеты (baseline 80/20 → 140 шт;
CGCNN 80/10/10 → 70 шт). Высокий R² у CGCNN при худших MAE/RMSE — артефакт большего
разброса таргета в его тесте. Честный сигнал — **val MAE CGCNN ≈ 0.583–0.587**, что
**равно** baseline 5-fold CV MAE = **0.583**.

**Вывод: структура (простая CGCNN) на 696 точках НЕ бьёт состав — идут вровень.**
Это согласуется с принципом «усложнять только если бьёт baseline». Узкое место —
**НЕ модель, а объём данных**: 696 структур микроскопически мало для GNN (CGCNN/ALIGNN
в статьях — 10k–100k+). Физически «нужна структура» верно, но на этих данных
структурный сигнал не проявляется.

### Увеличение данных: подключили C2DB (через JARVIS API)

JARVIS отдаёт 72 датасета; релевантные 2D, кроме dft_2d: **`c2db`** (3520 структур,
1115 полупроводников, gap=PBE), **`alex_pbe_2d_all`** (Alexandria 2D, ещё больше).
Всё через тот же API — свой парсер не нужен.

- [x] Добавлен источник `c2db` в `baseline_2d_bandgap.py` (`--source c2db`) и в
      `export_structures_for_alignn.py` (`--source c2db` → `alignn_data_c2db/`,
      zip `alignn_data_c2db.zip` ~500 КБ, 1115 структур).

**ВНИМАНИЕ:** dft_2d (OptB88vdW) и c2db (PBE) — РАЗНЫЕ функционалы, разные значения
щели. НЕ сливать в один датасет. Использовать c2db отдельно как более крупный,
внутренне согласованный набор.

**Composition-baseline на c2db (1115 полупроводников) — заметно сильнее:**

| Метрика | dft_2d (696) | c2db (1115) |
|---|---|---|
| R² | 0.558 | **0.719** |
| MAE | 0.599 | **0.457** |
| RMSE | 0.789 | **0.590** |
| CV MAE | 0.583 ± 0.041 | **0.445 ± 0.019** |

Больше данных → R² 0.56→0.72 и вдвое стабильнее CV. (Нельзя отделить «больше данных»
от «c2db чище» и «PBE легче предсказать по составу» — но baseline объективно мощнее.)

### CGCNN на c2db (1115): структура ВПЕРВЫЕ обогнала состав

Прогнали ту же ячейку CGCNN (PyG, T4) на `alignn_data_c2db` (1115 структур).

| Метрика | baseline (состав) | CGCNN (структура) |
|---|---|---|
| MAE  | 0.457 | **0.421** |
| RMSE | **0.590** | 0.651 |
| R²   | 0.719 | **0.805** |

Сравнивать честно по **MAE** (R²/RMSE на разных тест-сетах: 223 vs 112). val MAE CGCNN
≈ **0.42** против baseline CV MAE **0.445** → структура ниже на ~0.025 эВ (≈5–6%).

**КЛЮЧЕВОЕ:** на 696 структурах GNN был ВРОВЕНЬ с составом, на 1115 — ВПЕРВЫЕ обогнал.
Тренд подтверждает гипотезу «узкое место = данные, не модель»: при росте N структурный
сигнал проявляется. Направление живое.

### Масштаб: подключили Alexandria 2D (`alex_pbe_2d_all`, ~137k)

JARVIS-тег `alex_pbe_2d_all` = 137 833 структуры, таргет `band_gap_ind` (непрямая щель,
PBE). Фильтр стабильности `e_above_hull<=0.1` эВ/атом → **13 349 стабильных
полупроводников** (отсекли 118k гипотетических/нестабильных). Источник добавлен как
`alex_2d` в оба скрипта (`--source alex_2d`, фильтр `--ehull-max`, дефолт 0.1).
Экспорт: `alignn_data_alex_2d/` + `alignn_data_alex_2d.zip` (~5.4 МБ, 13349 структур).

**Composition-baseline растёт с объёмом данных (все 2D, полупроводники):**

| Датасет | N | R² | MAE | CV MAE |
|---|---|---|---|---|
| dft_2d (OptB88vdW) | 696 | 0.558 | 0.599 | 0.583 ± 0.041 |
| c2db (PBE) | 1115 | 0.719 | 0.457 | 0.445 ± 0.019 |
| **Alexandria 2D (PBE)** | **13349** | **0.873 / 0.892 (XGB)** | **0.356 / 0.340** | **0.360 ± 0.007** |

Тренд однозначный: данные — главный рычаг. На 13k baseline по составу уже R²≈0.89,
CV сжался до ±0.007. Это «не стыдно показать» уровень.

### CGCNN на Alexandria 13k: структура РАЗГРОМНО обогнала состав

Прогнали ту же ячейку CGCNN (PyG, T4) на `alignn_data_alex_2d` (13349 структур).

| Метрика | baseline состав | CGCNN структура |
|---|---|---|
| MAE  | 0.340–0.360 | **0.215** |
| RMSE | 0.483 | **0.390** |
| R²   | 0.892 | **0.943** |

MAE упал с ~0.35 до **0.215** (≈−40%). Тест-сеты большие (2670 vs ~1335) → артефакт
сплита уже не объясняет разрыв. Это настоящий уверенный выигрыш структуры.

**ИТОГ всей линии «GNN vs состав» (сравнение по MAE):**

| N | состав | CGCNN | кто |
|---|---|---|---|
| 696   | 0.583 | ~0.585 | вровень |
| 1115  | 0.445 | ~0.42  | структура +5–6% |
| 13349 | 0.360 | **0.215** | **структура +40%** |

Гипотеза «узкое место = объём данных, а не модель» ПОДТВЕРЖДЕНА. При малом N графу нечего
учить (вровень с Magpie); с ростом N структурный сигнал (геометрия слоёв) проявляется и
доминирует. Ради этого GNN и затевался.

### Масштаб vs качество: 26k (ehull≤0.2) ХУЖЕ для состава, чем 13k (ehull≤0.1)

Ослабили фильтр стабильности до 0.2 → 26561 полупроводников. Composition-baseline
ПРОСЕЛ:

| Датасет | N | RF R² | XGB MAE | CV MAE |
|---|---|---|---|---|
| Alexandria ehull≤0.1 | 13349 | 0.873 | 0.340 | **0.360 ± 0.007** |
| Alexandria ehull≤0.2 | 26561 | 0.848 | 0.405 | **0.419 ± 0.005** |

Причина: добавили 13k МЕТАСТАБИЛЬНЫХ структур, у которых щель сильнее зависит от
геометрии → для состава это шум, MAE вырос. CV-разброс при этом ещё сжался (±0.005).
**Урок: больше данных помогает только при сохранении качества; для composition-модели
13k стабильных — лучше, чем 26k со «шлаком».** Экспорт `alignn_data_alex_2d_eh02.zip`
(11 МБ, 26561 структур) готов.

**Гипотеза ПОДТВЕРЖДЕНА (CGCNN на 26k, T4):**

| N | состав MAE (CV) | CGCNN MAE | разрыв | % |
|---|---|---|---|---|
| 13349 (ehull≤0.1) | 0.360 | **0.215** (R²=0.943) | 0.145 | −40% |
| 26561 (ehull≤0.2) | 0.419 | **0.262** (R²=0.918) | 0.157 | −37% |

При 13k→26k состав ухудшился сильнее (+0.059), структура слабее (+0.047) → абсолютный
разрыв ВЫРОС (0.145→0.157). Метастабильные структуры, «ослепившие» состав, CGCNN
переварил лучше — прямое доказательство ценности структуры. НО даже CGCNN на 26k хуже,
чем на чистых 13k (0.262 vs 0.215): качество данных важно и для GNN.

**ЛУЧШИЙ результат проекта: CGCNN на 13k стабильных — MAE=0.215, R²=0.943.**
Дальнейшее ослабление стабильности — тупик (качество падает). Масштаб — только через
transfer learning с чистого 3D.

### Transfer learning 3D→2D: работает, но прирост скромный

Сделали на Kaggle полностью на jarvis API (без локального экспорта): претрейн CGCNN на
**dft_3d** (OptB88vdW, подвыборка 40k из 76k, 15 эпох, val MAE 3D ~0.29) → дотюн на
чистых 13k 2D (alex_2d, ehull≤0.1, band_gap_ind).

**Грабли Kaggle (важно):**
- joblib-параллельная сборка графов ВИСНЕТ (пиклит torch-тензоры между процессами).
  Строить графы СЕРИЙНО, с печатью прогресса. НЕ использовать joblib для PyG Data.
- 3D-графы при cutoff 8 Å плотные по рёбрам → эпохи тяжёлые. Ускорение: подвыборка 40k +
  batch 256 (не 64). Если совсем медленно — cutoff 5–6 Å (но тогда и 2D тем же cutoff).
- Память: рёбра хранить как расстояния (1 число), RBF считать в forward, иначе 76k×40d
  рёбер не влезает в RAM.

**Честное сравнение (один тест-сет 2D, batch 256):**

| Модель | MAE | RMSE | R² |
|---|---|---|---|
| CGCNN с нуля | 0.242 | 0.436 | 0.929 |
| CGCNN + 3D претрейн | **0.232** | **0.419** | **0.934** |
| baseline (состав) | ~0.360 | — | — |

Все три метрики улучшились согласованно → выигрыш реальный, но малый (~4%). На чистых 13k
2D модель уже у потолка (R²≈0.93), поэтому лёгкий 3D-претрейн добавляет немного.
Прим.: «с нуля» тут 0.242 (batch 256) против рекорда 0.215 (batch 64) — меньший батч
генерализует чуть лучше. **Абсолютный рекорд проекта остаётся CGCNN на 13k, batch 64,
MAE=0.215, R²=0.943.**

**Вывод:** лёгкие рычаги данных исчерпаны (696→13k дал MAE 0.58→0.21; transfer +дотюн дал
ещё ~4%). Дальше — убывающая отдача. Модель MAE≈0.23 эВ — это уже рабочий инструмент.

### Анализ ошибок CGCNN (дотюн с 3D, MAE=0.228, MC-dropout unc)

Ячейка на Kaggle: дотюн → детерминированные предсказания на тесте → MC-dropout (30
проходов, BatchNorm выкл) → метаданные из z (natoms, nelem, maxZ, has_TM) → разбивка
ошибки + 4 графика (parity, остатки, калибровка, boxplot по химии).

**КОНТРИНТУИТИВНЫЙ результат — ошибка от плотности данных, а не от физики:**

| Срез | MAE |
|---|---|
| с переходными металлами | **0.199** |
| без TM | 0.26 |
| тяжёлые Z≥36 | **0.204** |
| лёгкие Z<36 | 0.335 |
| по бинам щели 0→10 эВ | ровно 0.21–0.25 |

Модель ТОЧНЕЕ на переходных металлах/тяжёлых (TMD типа MoS₂ — их в Alexandria густо,
модель набила руку) и ХУЖЕ на лёгких main-group (бориды/нитриды/оксиды — разнообразны,
каждого мало → длинный хвост). Вывод: ошибка определяется ПЛОТНОСТЬЮ обучающих примеров
в данной химии, а не сложностью физики. Ошибка почти НЕ зависит от величины щели
(модель не «заваливает» большие gap).

**Неопределённость:** `corr(unc,|err|)=0.17` — слабоположительная. MC-dropout чуть
чувствует, где ошибается, но калибровка слабая. Для надёжного «фильтра доверия» нужен
АНСАМБЛЬ (3–5 моделей, std), а не dropout на одном слое.

### Ансамбль для неопределённости (СДЕЛАНО)

5 CGCNN с разными сидами (seed 0–4), test MAE = 0.233/0.251/0.240/0.246/0.240. Веса в
`cgcnn_2d_ensemble.pt` (state_dicts всех 5 + mean/std). `screen_bandgap.py` автодетектит
его: неопределённость = std предсказаний между моделями (ДЕТЕРМИНИРОВАННАЯ, воспроизводимая,
калибруется лучше MC-dropout). Пример: арсенен As → 0.810 ± 0.048 эВ, стабильно.

### Тип щели прямая/непрямая (СДЕЛАНО, с уроком)

Данные: Alexandria хранит band_gap_ind и band_gap_dir; прямозонный = (dir−ind<0.1 эВ).
Баланс: **79% прямозонных / 21% непрямозонных** (baseline «всегда большинство» = 79.5%).

- ПОПЫТКА 1 (мультирегрессия [ind,dir], тип из разности): acc=0.787 = ТРИВИАЛЬНЫЙ
  baseline. Разность двух предсказаний с MAE~0.23 тонет в шуме. НЕ работает.
- ПОПЫТКА 2 (отдельная классиф. голова + pos_weight на редкий класс): **ROC-AUC=0.803**,
  balanced_acc=0.695, F1(непрям)=0.506. Структура РЕАЛЬНО предсказывает тип.
  Цена: мультизадачность просадила MAE щели 0.233→0.286.

**Решение для инструмента:** РАЗВЕСТИ задачи. Число gap — из ансамбля (MAE 0.23). Тип —
из классификатора `cgcnn_2d_typed.pt`. Не теряем ни точность, ни тип. Метрика accuracy
при дисбаласе бесполезна — мерить ROC-AUC / balanced_acc / F1 редкого класса.

### Внешняя валидация на эксперименте (СДЕЛАНО) — `validate_experiment.py`

7 каноничных монослоёв (структуры из dft_2d) → модель (PBE) vs эксп. оптич. щели:

| Материал | модель | эксп | Δ | unc |
|---|---|---|---|---|
| MoS2 | 1.67 | 1.88 | +0.21 | ±0.06 |
| MoSe2 | 1.50 | 1.55 | +0.05 | ±0.10 |
| WS2 | 1.87 | 2.00 | +0.13 | ±0.04 |
| WSe2 | 1.50 | 1.65 | +0.15 | ±0.12 |
| h-BN | 4.81 | 6.00 | +1.19 | ±0.27 |
| фосфорен | 1.22 | 2.00 | +0.78 | ±0.45 |
| графен | 2.09 | 0.00 | −2.09 | ±0.39 |

**Выводы (всё физически осмысленно):**
- PBE занижает на **+0.42 эВ (~15%)**. Лин. поправка `exp≈1.26·gap_PBE−0.12` → MAE 0.42→0.22.
- TMD близки (экситон в 2D гасит часть занижения); h-BN занижен сильно (широкая щель).
- **Граница честности:** модель училась ТОЛЬКО на полупроводниках → на графене (металл)
  выдаёт мусор 2.09 эВ. НО неопределённость это ловит: у проблемных (графен ±0.39,
  фосфорен ±0.45) unc втрое выше, чем у точных TMD (±0.04–0.12). Ансамбль-unc работает
  как фильтр доверия — подтверждено на out-of-distribution.
- Классификатор типа: TMD верно (прямая), h-BN ошибся (OOD широкозонный).

ИТОГ: для надёжной работы на произвольных структурах нужен upstream метал/п-проводник
классификатор (двухэтапная схема). Сейчас инструмент честен В ПРЕДЕЛАХ полупроводников.

### Дальше по проекту (фокус смещается с метрики на ценность)
1. **Применение (главное):** screening-пайплайн (структура→gap по списку кандидатов) +
   мини-морда (Gradio/Streamlit). Опционально LLM-обёртка: модель как tool для
   естественно-языковых запросов («2D-полупроводники с gap ~1.5 эВ»). Модель — численный
   бэкенд, LLM — интерфейс. Голая модель уже полезна, LLM = слой UX, не необходимость.
2. **Калибровка неопределённости через ансамбль** (3–5 CGCNN) — для лаб критично «где не
   доверять».
3. **Закрыть слабое место:** добор данных по лёгким main-group 2D (где модель слепнет).
4. **Расширить таргет:** тип щели (прямая/непрямая), эффективная масса, работа выхода —
   для наноэлектроники полезнее, чем третий знак gap.
5. **Внешняя валидация на эксперименте** (PBE занижает gap на ~30–50% — показать
   понимание этого = заявленное «понимание физики»).
6. **Поговорить с 5–10 людьми из лабораторий** (наноэлектроника/2D): рабочий пайплайн
   MAE 0.2 эВ как точка входа в разговор.

## Технические заметки

- GPU для baseline НЕ нужен — scikit-learn/XGBoost считают на CPU за минуты.
- GPU (Kaggle) понадобится только на этапе GNN.
- Электроотрицательность ожидаемо входит в топ важных признаков для band gap —
  это физически корректный sanity-check, что фичи осмысленные.
- На маке использовать `python3`/`pip3`, внутри активированного venv — просто
  `python`/`pip`.
